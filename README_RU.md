# Competition Monitor - Мониторинг конкурентов

Desktop приложение для анализа сайтов конкурентов с использованием Selenium парсинга, FastAPI backend и PyQt6 UI.

## 🎯 Возможности

- ✅ Парсинг сайтов в реальном времени через Selenium (headless Chrome)
- ✅ FastAPI backend с отслеживанием истории
- ✅ Ограничение конкурентных запросов через Semaphore
- ✅ Desktop UI на PyQt6
- ✅ Сборка в .exe файл через PyInstaller

## 🛠️ Технологии

- Python 3.10+
- PyQt6 (Desktop UI)
- FastAPI + Uvicorn (Backend API)
- Selenium (Web парсинг)
- OpenAI API (Анализ скриншотов)
- PyInstaller (Сборка .exe)

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd competition-monitor
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
pip install -r desktop/requirements.txt
```

### 4. Настройка .env файла

Создайте файл `.env` в корне проекта:

```env
OPENAI_API_KEY=your-api-key-here
OPENAI_PROXY=http://user:pass@ip:port  # опционально
OPENAI_MODEL=gpt-4o-mini
```

## 🚀 Запуск

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
python start_dev.py
```

Этот скрипт автоматически запустит backend и desktop приложение.

### Вариант 2: Ручной запуск

**Терминал 1 - Backend:**
```bash
uvicorn backend.main:app --reload
```

**Терминал 2 - Desktop:**
```bash
python desktop/main.py
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Установите переменную окружения для тестового режима
set TESTING=True

# Запустите тесты
pytest tests/test_integration.py -v
```

### Проверка API

Откройте в браузере:
- API Docs: http://127.0.0.1:8000/docs
- Health Check: http://127.0.0.1:8000/health

### Тестовый запрос парсинга

```bash
curl "http://127.0.0.1:8000/parsedemo?url=https://example.com"
```

## 📦 Сборка .exe

```bash
python build.py
```

Результат: `dist/competitionmonitor.exe`

### ⚠️ Важные замечания по .exe:

1. **Google Chrome обязателен**: На целевой машине должен быть установлен Google Chrome
2. **Первый запуск**: Файл `history.json` создастся автоматически рядом с .exe
3. **Портативность**: .exe можно переносить на другие Windows машины (с установленным Chrome)
4. **Размер**: ~50-150 MB (зависит от включенных модулей)

## 📁 Структура проекта

```
competition-monitor/
├── backend/                 # FastAPI backend
│   ├── services/           # Сервисы (парсинг, OpenAI, история)
│   ├── models/             # Pydantic модели
│   ├── config.py           # Конфигурация
│   ├── schemas.py          # API схемы
│   └── main.py             # Главный файл API
├── desktop/                # PyQt6 desktop приложение
│   ├── main.py             # Главное окно
│   ├── api_client.py       # HTTP клиент для API
│   ├── styles.py           # Стили UI
│   └── requirements.txt    # Зависимости desktop
├── tests/                  # Тесты
│   ├── conftest.py         # Pytest конфигурация
│   └── test_integration.py # Интеграционные тесты
├── .env                    # Переменные окружения
├── requirements.txt        # Зависимости backend
├── build.py                # Скрипт сборки .exe
├── start_dev.py            # Скрипт запуска в dev режиме
├── README.md               # Документация (EN)
├── README_RU.md            # Документация (RU)
└── TESTING.md              # Руководство по тестированию

```

## 🔧 Конфигурация

### Backend (backend/config.py)

- `OPENAI_API_KEY` - API ключ OpenAI (обязательно)
- `OPENAI_PROXY` - HTTP/SOCKS5 прокси (опционально)
- `OPENAI_MODEL` - Модель GPT (по умолчанию: gpt-4o-mini)
- `history_file` - Файл истории (по умолчанию: history.json)
- `max_history_items` - Максимум записей в истории (по умолчанию: 50)

### Desktop (desktop/api_client.py)

- `base_url` - URL backend API (по умолчанию: http://127.0.0.1:8000)

## 🐛 Отладка проблем

### "ModuleNotFoundError" при запуске .exe
**Решение:** Проверьте `build.py`, убедитесь что все модули включены

### Черное консольное окно появляется
**Решение:** Проверьте опции Chrome в `parsingservice.py` (должен быть `log_path=os.devnull`)

### Chrome не найден
**Решение:** Установите Google Chrome на целевой машине

### Не удается подключиться к серверу
**Решение:** Убедитесь, что backend запущен на http://127.0.0.1:8000

### История не сохраняется
**Решение:** Проверьте права на запись в директорию с .exe

## 📝 API Endpoints

### GET /health
Проверка работоспособности сервера

**Ответ:**
```json
{
  "status": "ok",
  "service": "competitor-analysis"
}
```

### GET /parsedemo
Парсинг сайта конкурента

**Параметры:**
- `url` (query, optional) - URL для парсинга

**Ответ:**
```json
{
  "url": "https://example.com",
  "data": {
    "product_name": "Leather Tote Aurora",
    "price": "350 USD",
    "material": "Full-grain leather",
    "description": "Premium Italian leather bag...",
    "image_url": "https://example.com/image.jpg",
    "parsed_at": "2024-01-01T12:00:00"
  },
  "history": [...]
}
```

### POST /analyze
Анализ скриншота через OpenAI Vision

**Тело запроса:**
```json
{
  "base64_image": "data:image/png;base64,..."
}
```

**Ответ:**
```json
{
  "design_score": 8,
  "material_quality_focus": 7.5,
  "lifestyle_context_score": 9.0,
  "summary": "Анализ дизайна..."
}
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Ваше имя / Команда

## 📞 Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.
