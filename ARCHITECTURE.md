# 🏗️ Архитектура Competition Monitor

## 📊 Общая схема

```
┌─────────────────────────────────────────────────────────────────┐
│                        COMPETITION MONITOR                       │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────┐         ┌──────────────────┐
│   Desktop UI     │◄───────►│   Backend API    │
│    (PyQt6)       │  HTTP   │    (FastAPI)     │
└──────────────────┘         └──────────────────┘
        │                            │
        │                            ├──────────────┐
        │                            │              │
        ▼                            ▼              ▼
┌──────────────────┐         ┌──────────────┐  ┌──────────────┐
│   User Input     │         │   Selenium   │  │  OpenAI API  │
│   - URL          │         │   WebDriver  │  │   Vision     │
│   - Settings     │         │   (Chrome)   │  │   Analysis   │
└──────────────────┘         └──────────────┘  └──────────────┘
                                     │
                                     ▼
                             ┌──────────────┐
                             │  Competitor  │
                             │   Website    │
                             └──────────────┘
```

## 🔄 Поток данных

### 1. Парсинг сайта конкурента

```
User Input (URL)
    │
    ▼
Desktop UI (main.py)
    │
    │ HTTP GET /parsedemo?url=...
    ▼
Backend API (main.py)
    │
    ▼
Parsing Service (parsingservice.py)
    │
    ├─► Selenium WebDriver
    │       │
    │       ▼
    │   Chrome Browser (headless)
    │       │
    │       ▼
    │   Competitor Website
    │       │
    │       ▼
    │   HTML Parsing
    │       │
    │       ▼
    │   Extract Data:
    │   - Product Name
    │   - Price
    │   - Material
    │   - Description
    │   - Image URL
    │
    ▼
History Service (history_service.py)
    │
    ▼
Response to Desktop UI
    │
    ▼
Display Results
```

### 2. Анализ скриншота (опционально)

```
Screenshot (base64)
    │
    ▼
Desktop UI
    │
    │ HTTP POST /analyze
    ▼
Backend API
    │
    ▼
OpenAI Service (openai_service.py)
    │
    ├─► HTTP Client (httpx)
    │       │
    │       ├─► Proxy (optional)
    │       │
    │       ▼
    │   OpenAI API
    │       │
    │       ▼
    │   GPT-4 Vision Analysis
    │       │
    │       ▼
    │   JSON Response:
    │   - design_score
    │   - material_quality_focus
    │   - lifestyle_context_score
    │   - summary
    │
    ▼
Response to Desktop UI
    │
    ▼
Display Analysis
```

## 🧩 Компоненты

### Desktop Layer (PyQt6)

```
┌─────────────────────────────────────────┐
│         CompetitorMonitorApp            │
│  (desktop/main.py)                      │
├─────────────────────────────────────────┤
│  Components:                            │
│  - QLineEdit (URL input)                │
│  - QTextEdit (Prompt input)             │
│  - QPushButton (Analyze button)         │
│  - QTextEdit (Results display)          │
│                                         │
│  Worker Thread:                         │
│  - AnalysisWorker (QThread)             │
│    └─► Async API calls                  │
│                                         │
│  API Client:                            │
│  - APIClient (api_client.py)            │
│    └─► HTTP requests to backend         │
│                                         │
│  Styles:                                │
│  - DARK_THEME (styles.py)               │
└─────────────────────────────────────────┘
```

### Backend Layer (FastAPI)

```
┌─────────────────────────────────────────┐
│           FastAPI Application           │
│  (backend/main.py)                      │
├─────────────────────────────────────────┤
│  Endpoints:                             │
│  - GET  /health                         │
│  - GET  /parsedemo?url=...              │
│  - POST /analyze                        │
│  - GET  /docs (Swagger UI)              │
│                                         │
│  Middleware:                            │
│  - CORS (allow all origins)             │
│                                         │
│  Configuration:                         │
│  - Settings (config.py)                 │
│    └─► Environment variables (.env)     │
└─────────────────────────────────────────┘
```

### Service Layer

```
┌─────────────────────────────────────────┐
│         Parsing Service                 │
│  (services/parsingservice.py)           │
├─────────────────────────────────────────┤
│  - Selenium WebDriver initialization    │
│  - Headless Chrome configuration        │
│  - HTML element extraction              │
│  - Fallback to demo data                │
│  - Semaphore (max 3 concurrent)         │
│  - History tracking                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         OpenAI Service                  │
│  (services/openai_service.py)           │
├─────────────────────────────────────────┤
│  - AsyncOpenAI client                   │
│  - Proxy support (httpx)                │
│  - Vision API integration               │
│  - JSON response parsing                │
│  - Error handling                       │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│         History Service                 │
│  (services/history_service.py)          │
├─────────────────────────────────────────┤
│  - In-memory storage                    │
│  - Thread-safe operations               │
│  - Max 50 entries                       │
│  - JSON serialization                   │
└─────────────────────────────────────────┘
```

## 🔐 Безопасность

### Конфигурация

```
┌─────────────────────────────────────────┐
│         Environment Variables           │
│  (.env file)                            │
├─────────────────────────────────────────┤
│  OPENAI_API_KEY=sk-...                  │
│  OPENAI_PROXY=http://...                │
│  OPENAI_MODEL=gpt-4o-mini               │
│                                         │
│  Security:                              │
│  - Not in git (.gitignore)              │
│  - Loaded via pydantic-settings         │
│  - Validated on startup                 │
└─────────────────────────────────────────┘
```

### CORS Policy

```
┌─────────────────────────────────────────┐
│         CORS Middleware                 │
├─────────────────────────────────────────┤
│  allow_origins: ["*"]                   │
│  allow_credentials: True                │
│  allow_methods: ["*"]                   │
│  allow_headers: ["*"]                   │
│                                         │
│  Note: For production, restrict origins │
└─────────────────────────────────────────┘
```

## ⚡ Производительность

### Concurrency Control

```
┌─────────────────────────────────────────┐
│         Semaphore (max 3)               │
├─────────────────────────────────────────┤
│                                         │
│  Request 1 ──┐                          │
│  Request 2 ──┼──► [Semaphore] ──► Chrome│
│  Request 3 ──┘                          │
│  Request 4 ──► [Waiting...]             │
│  Request 5 ──► [Waiting...]             │
│                                         │
│  Benefits:                              │
│  - Prevents Chrome overload             │
│  - Limits memory usage                  │
│  - Ensures stability                    │
└─────────────────────────────────────────┘
```

### Async Operations

```
┌─────────────────────────────────────────┐
│         Async Flow                      │
├─────────────────────────────────────────┤
│                                         │
│  Desktop UI (QThread)                   │
│      │                                  │
│      ▼                                  │
│  HTTP Request (async)                   │
│      │                                  │
│      ▼                                  │
│  FastAPI (async endpoint)               │
│      │                                  │
│      ▼                                  │
│  asyncio.to_thread(selenium)            │
│      │                                  │
│      ▼                                  │
│  Selenium (blocking)                    │
│                                         │
│  Benefits:                              │
│  - Non-blocking UI                      │
│  - Efficient resource usage             │
│  - Better user experience               │
└─────────────────────────────────────────┘
```

## 📦 Deployment

### Development Mode

```
┌─────────────────────────────────────────┐
│         Development Setup               │
├─────────────────────────────────────────┤
│                                         │
│  Terminal 1:                            │
│  uvicorn backend.main:app --reload      │
│      │                                  │
│      ▼                                  │
│  Backend API (http://127.0.0.1:8000)    │
│                                         │
│  Terminal 2:                            │
│  python desktop/main.py                 │
│      │                                  │
│      ▼                                  │
│  Desktop UI (PyQt6 window)              │
│                                         │
│  Or use: python start_dev.py            │
└─────────────────────────────────────────┘
```

### Production Build (.exe)

```
┌─────────────────────────────────────────┐
│         PyInstaller Build               │
├─────────────────────────────────────────┤
│                                         │
│  python build.py                        │
│      │                                  │
│      ▼                                  │
│  PyInstaller                            │
│      │                                  │
│      ├─► Collect Python modules         │
│      ├─► Collect Selenium drivers       │
│      ├─► Bundle dependencies            │
│      ├─► Create single .exe             │
│      │                                  │
│      ▼                                  │
│  dist/competitionmonitor.exe            │
│      │                                  │
│      ▼                                  │
│  Portable Windows application           │
│  (requires Chrome on target machine)    │
│                                         │
│  Size: ~50-150 MB                       │
└─────────────────────────────────────────┘
```

## 🧪 Testing

### Test Architecture

```
┌─────────────────────────────────────────┐
│         Test Suite                      │
│  (tests/)                               │
├─────────────────────────────────────────┤
│                                         │
│  conftest.py                            │
│  └─► Pytest fixtures                    │
│      └─► Test client setup              │
│                                         │
│  test_integration.py                    │
│  ├─► test_health_check()                │
│  ├─► test_parsedemo_endpoint()          │
│  └─► test_concurrent_requests()         │
│                                         │
│  Environment:                           │
│  TESTING=True                           │
│  └─► Mock Selenium responses            │
│                                         │
│  Run: pytest tests/ -v                  │
└─────────────────────────────────────────┘
```

## 🔧 Configuration Management

```
┌─────────────────────────────────────────┐
│         Configuration Flow              │
├─────────────────────────────────────────┤
│                                         │
│  .env file                              │
│      │                                  │
│      ▼                                  │
│  pydantic-settings                      │
│      │                                  │
│      ▼                                  │
│  Settings class (config.py)             │
│      │                                  │
│      ├─► Validation                     │
│      ├─► Type checking                  │
│      ├─► Default values                 │
│      │                                  │
│      ▼                                  │
│  Application configuration              │
│      │                                  │
│      ├─► OpenAI API key                 │
│      ├─► Proxy settings                 │
│      ├─► Model selection                │
│      └─► History limits                 │
└─────────────────────────────────────────┘
```

## 📊 Data Models

### Pydantic Schemas

```
┌─────────────────────────────────────────┐
│         Request/Response Models         │
│  (backend/schemas.py)                   │
├─────────────────────────────────────────┤
│                                         │
│  AnalyzeRequest                         │
│  └─► base64_image: str                  │
│                                         │
│  AnalyzeResponse                        │
│  ├─► design_score: int (0-10)           │
│  ├─► material_quality_focus: float      │
│  ├─► lifestyle_context_score: float     │
│  └─► summary: str                       │
│                                         │
│  Benefits:                              │
│  - Automatic validation                 │
│  - Type safety                          │
│  - API documentation                    │
│  - JSON serialization                   │
└─────────────────────────────────────────┘
```

## 🚀 Scalability Considerations

### Current Limitations

```
┌─────────────────────────────────────────┐
│         Scalability Limits              │
├─────────────────────────────────────────┤
│                                         │
│  Concurrency: 3 requests (Semaphore)    │
│  History: 50 entries (in-memory)        │
│  Storage: No persistent database        │
│  Deployment: Single machine only        │
│                                         │
│  For production scale:                  │
│  - Add Redis for history                │
│  - Use PostgreSQL for persistence       │
│  - Deploy with Docker/K8s               │
│  - Add load balancer                    │
│  - Implement caching                    │
└─────────────────────────────────────────┘
```

## 📝 Logging

```
┌─────────────────────────────────────────┐
│         Logging Architecture            │
├─────────────────────────────────────────┤
│                                         │
│  Python logging module                  │
│      │                                  │
│      ├─► Backend (config.py)            │
│      │   └─► INFO level                 │
│      │   └─► StreamHandler (stdout)     │
│      │                                  │
│      ├─► Selenium                       │
│      │   └─► Suppressed (log_level=3)   │
│      │   └─► Output to os.devnull       │
│      │                                  │
│      └─► Desktop UI                     │
│          └─► Console output             │
│                                         │
│  For production:                        │
│  - Add file handlers                    │
│  - Implement log rotation               │
│  - Add structured logging               │
└─────────────────────────────────────────┘
```

---

## 🎯 Заключение

Архитектура Competition Monitor построена на принципах:

1. **Модульность** - четкое разделение слоев (UI, API, Services)
2. **Асинхронность** - неблокирующие операции для лучшего UX
3. **Безопасность** - конфигурация через переменные окружения
4. **Масштабируемость** - готовность к расширению функционала
5. **Тестируемость** - изолированные компоненты с mock-поддержкой

Для более детальной информации см.:
- [README_RU.md](README_RU.md) - полная документация
- [PROJECT_STATUS.md](PROJECT_STATUS.md) - текущий статус
- [CONTRIBUTING.md](CONTRIBUTING.md) - руководство разработчика
